name: Deploy and Update Docs

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to server
      uses: appleboy/ssh-action@v0.1.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          cd ~/airbureau
          
          # Останавливаем сервис перед обновлением
          echo "🛑 Stopping bybit-linear-ticker service..."
          sudo systemctl stop bybit-linear-ticker.service || true
          
          # Обновляем код
          echo "📥 Pulling latest code..."
          git pull origin main
          
          # Обновляем виртуальное окружение и зависимости
          if [ ! -d "venv" ]; then
            python3 -m venv venv
          fi
          
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt
          
          # Обновляем unit-файл сервиса (если изменился)
          echo "🔄 Updating systemd service..."
          sudo cp -f systemd/bybit-linear-ticker.service /etc/systemd/system/ || true
          sudo systemctl daemon-reload
          
          # Перезапускаем сервис
          echo "🚀 Starting bybit-linear-ticker service..."
          sudo systemctl start bybit-linear-ticker.service
          sudo systemctl enable bybit-linear-ticker.service
          
          # Проверяем статус
          echo "✅ Checking service status..."
          sudo systemctl status bybit-linear-ticker.service --no-pager
          
          echo "🎉 Deployment completed successfully!"